# 参与开发

先阅读 `docs/ENGINEERING_GUIDE.md`，为修改建立一个边界明确的 Issue。提交内容应说明解决的工程问题、修改的模型或接口、预期行为，以及实际执行的验证。

修改能源方程时，补充与独立手算或物理边界对应的检查；修改文案时无需增加重复实现的测试。参数、字段名称、观测或动作维度变化要同步更新文档，并明确旧模型是否还能使用。

结果图必须能追溯到数据、配置和实验日志。不要提交真实用户或园区敏感数据、认证信息，也不要将短训练图写成算法收敛或商业收益证明。

提交 PR 前至少运行与改动相关的检查。能源模型或环境接口有变动时运行：

```bash
python -m unittest discover -s tests -v
```
